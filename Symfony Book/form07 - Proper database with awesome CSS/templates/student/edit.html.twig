{% extends 'base.html.twig' %}

{% block body %}
    <h1>Edit Category</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Edit">
    {{ form_end(form) }}

<ul>
    <li>
        <a href="{{ path('list.html.twig') }}">back to list</a>
    </li>

</ul>

{% endblock %}
